Option Explicit

Private Sub CheckBox1_Click()

End Sub

Private Sub Label1_Click()

End Sub

Private Sub CommandButton1_Click()
    With ListBox1
        .AddItem ComboBox1.Value
    End With
End Sub

Private Sub CommandButton2_Click()
    Dim lb As MSForms.ListBox
    Dim Item As Variant
    Dim OutputFilePath As String
    Dim OutputFile As Integer
    Dim i As Long

    ' リストボックスの参照を取得
    Set lb = UserForm1.ListBox1 ' リストボックスの名前を適宜変更
    
    ' 出力ファイルのパスを指定
    OutputFilePath = ThisWorkbook.Path & "\a.txt" ' 出力先のパスを適宜変更
    
    ' テキストファイルを新規作成または上書き
    OutputFile = FreeFile
    Open OutputFilePath For Output As OutputFile
    
    For i = 0 To lb.ListCount - 1
    If Not lb.Selected(i) Then
        Print #OutputFile, lb.List(i, 1) & vbTab & lb.List(i, 2)
    End If
    Next
        
    ' テキストファイルを閉じる
    Close OutputFile
  
End Sub


Private Sub CommandButton3_Click()
    Dim FilePath As String
    Dim Text As String
    Dim FileContent As String
    
    ' ファイルのパスを指定
    FilePath = ThisWorkbook.Path & "\a.txt" ' 読み取るファイルのパスを適宜変更
    
    ' ファイルを開く
    Open FilePath For Input As #1
    
    With ListBox1
        ' 行ごとにテキストを読み取り
        Do Until EOF(1)
            Line Input #1, Text
            .AddItem Text
        Loop
    End With
    
    
    
    ' ファイルを閉じる
    Close #1
    

End Sub

Private Sub ListBox1_Click()
End Sub

Private Sub UserForm_Click()

End Sub


Private Sub UserForm_Initialize()
        
    With ComboBox1
        .AddItem "物理"
        .AddItem "物理"
    End With
    
  With ListBox1
    .ColumnCount = 3 '列数を設定
    .AddItem "" '空欄のリストを追加
    .List(0, 0) = "鈴木" '1列目
    .List(0, 1) = 190 '2列目
    .List(0, 2) = 70 '3列目
  End With
    
    
End Sub
